# Dockerfile.bridge
FROM python:3.10-slim

WORKDIR /app

COPY backend/requirements.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY dashboard_bridge.py /app/dashboard_bridge.py

CMD ["uvicorn", "dashboard_bridge:app", "--host", "0.0.0.0", "--port", "8001"]
