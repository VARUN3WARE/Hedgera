# Local CI

Wrapper scripts for backend tests. Full guide: [docs/testing.md](../../docs/testing.md).

```bash
./scripts/ci/run-tests.sh        # fast (excludes finrl/slow)
./scripts/ci/run-tests-full.sh   # everything
```

GitHub Actions is not enabled yet.
