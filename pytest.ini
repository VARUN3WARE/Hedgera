[pytest]
testpaths = backend/tests
pythonpath = .
asyncio_mode = auto
markers =
    slow: long-running tests (models, training, large data)
    finrl: FinRL, torch, or model artifact dependencies
    integration: crosses service boundaries (Redis, API, pipeline)
addopts = -ra
